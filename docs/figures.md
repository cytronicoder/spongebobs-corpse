### Figures

All final figures are generated through `viz.plots` and exported through `io.save.save_figure`:

- `batch_analysis_plot` (panel + right gutter fit summary)
- `batch_cv_plot`
- `residual_plots`

Every final figure is exported as PNG + PDF and has `_caption.txt` sidecar metadata.
